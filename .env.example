# Groq — https://console.groq.com/keys
GROQ_API_KEY=
# Optional: override the default model. This is a runtime default read from env;
# if Groq deprecates it, generation fails at request time — keep this current.
GROQ_MODEL=llama-3.3-70b-versatile

# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
